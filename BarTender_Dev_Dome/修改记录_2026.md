# 2026 修改记录

## 2026-06-08

### 12251 标签生成逻辑

- 12251 标签暂时统一使用 `2.btw` 模板。
- 新增 `EWM` 字段写入，内容格式为 `客户型号,PO号`。
- 保留并延续旧的 12251 标签字段生成逻辑，包括 `12251资料.xlsx` 查找和 `CPXX`、`CPCD`、`CPXX-2`、`IPDJ`、`SW`、`WS`、`PO` 等字段填充。
- 工字标、品名标的标签规格筛选改为仅按客户编码 `12251`、`19079` 判断，不再要求规格名称包含“客户”两个字。

